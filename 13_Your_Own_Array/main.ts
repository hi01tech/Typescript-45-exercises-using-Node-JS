let transportation: string [] = [ "Car" ,"Bike", 'Bicycle', "Scooty"];

// FOR LOOP METHOD:

for( let i=0 ; i < transportation.length ; ++i) {
    console.log (`I would like to own a ${transportation[i]} as because it makes travelling easy.`)
}